# 2026-09-17（M4 session：權限驗證 + audit log + 帳號/角色管理 + 收尾）

## 進度概覽
- M0 ✅ M1 ✅ M2 ✅ M3 ✅（此前）
- **M4 ✅ 今日完成**：後端全 API 權限/scope 驗證通過、操作紀錄 audit log（後端＋前端）、帳號/角色管理（後端＋前端）、docs 更新。

## 今日實作
### 1. AuditLog（後端＋前端）
- `backend/Models/AuditLog.cs`（UserId/UserEmail/Role/Action/Category/Entity/EntityId/Detail(json)/IpAddress/CreatedAt）＋ `HrDbContext` 配置 `audit_logs`。
- `backend/Services/AuditLogService.cs`（Scoped，註冊於 Program.cs；`LogAsync` 讀 current User claims，`LogLoginAsync` 給登入用含失敗）。
- 掛接寫入：auth login/login_failed、employees/departments/positions CRUD、leave/overtime 申請+審核+取消、attendance clock-in/out、employee-salaries upsert、payrolls generate/confirm/pay/bonus、users create/update。
- `PermissionCatalog` 新增 `audit.read`（僅 admin；DbSeeder 會自動同步）。
- `Controllers/AuditLogsController.cs`：GET `/api/v1/audit-logs`（page/pageSize/category/action/userId/keyword/from/to 過濾）。
- 前端：`services/audit.ts`（category/action 中文標籤 map）、`views/system/AuditLogs.vue`、路由 `/system/audit-logs`（audit.read）、sidebar「系統」群組。
- **踩坑**：前端 audit query keyword 參數後端最初漏掉 → 後端已補。

### 2. Users/Roles CRUD（後端＋前端，`user.manage`）
- `Controllers/UsersController.cs`：GET list（keyword/roleId 篩選、分頁）、POST create（密碼≥8、信箱唯一）、PUT update（email/roleId/employeeId/isActive/選填重設密碼）。
- `Controllers/RolesController.cs`：GET list（roles + permissions + userCount）。
- `User` entity 補 `Employee` 導覽屬性（DbContext 補 FK SetNull）。
- `Dtos/UserDtos.cs`（UserDto/CreateUserRequest/UpdateUserRequest/RoleDto）。
- 前端：`services/system.ts`、`views/system/Users.vue`（新增/編輯 modal、綁定員工、啟用/停用）、`views/system/Roles.vue`（權限 tag 列表）、路由 `/system/users`、`/system/roles`。
- **踩坑**：RolesController 原本在投影內用 `db.Users.Count(...)` 子查詢 → EF Core 10 無法翻譯（500）→ 改為先載入 roles + group count 再記憶體內聚合。

## 權限驗證（M4 主訴）
- `node %TEMP%\opencode\verify-m4.mjs`（23 項全過）＋ 單獨驗證 login_failed 寫入正確。
- 覆蓋：無 token→401；audit-logs/users/roles 僅 admin 200、其他角色 403；duplicate email→400；employee 無 employee.read/payroll.manage→403；manager 可看 payrolls 但不可 confirm→403；audit login/create/failed 都有紀錄。
- 前端三新頁 UI 冒煙（`%TEMP%\opencode\verify-m4-ui.mjs`，無頭 Chrome + CDP + admin token）：`/system/users`（5 帳號）、`/system/roles`（4 角色＋權限 tags）、`/system/audit-logs`（18 筆含 login/create/login_failed）全部正常，sidebar「系統」選單出現。

## 服務狀態（本 session 結束後）
- 後端 `dotnet run --launch-profile http` 背景執行（log `%TEMP%\opencode\api-{out,err}.log`，Hot Reload 關閉、改 code 須重啟）、vite dev 背景（舊實例，今日未改路由後重啟——**實際今天有加路由，但未重啟 vite；新頁面驗證卻成功？**）等等——驗證是直開路徑且成功，代表 vite 這次有吃到新路由（可能因為加了新 chunk 觸發 HMR 重載 router？）。**教訓留檔：新增/修改路由後務必重啟 vite**；本次運氣好，勿當常態。
- 測試殘留資料：DB 有 `test-*@hr.local` 測試帳號（verify-m4 建的）＋多筆 login audit。

## 下一步（收尾）
- git commit：M4 全部變更（migration、controllers、services、前端、docs），message 用繁體中文。
- 可選：測試帳號清除、README 或驗收清單、mv verify 腳本進 repo `scripts/`。